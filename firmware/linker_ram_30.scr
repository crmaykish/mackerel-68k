MEMORY
{
	RAM(RX) : ORIGIN = 0x80000000, LENGTH = 0x08000000 - 0x4000 # 16KB reserved for stack
}

SECTIONS
{
	.text 0x80000000 : { *(.text) } > RAM
	.rodata : { *(.rodata) } > RAM
	.bss : { *(.bss) } > RAM
}
