MEMORY
{
	RAM(RX) : ORIGIN = 0x400, LENGTH = 0x80000 - 0x400 - 0x4000 #ignore the RAM used by the bootloader
}

SECTIONS
{
	.text 0x400 : { *(.text) } > RAM
	.rodata : { *(.rodata) } > RAM
	.bss : { *(.bss) } > RAM
}
