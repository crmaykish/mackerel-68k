GAL16V8
IODEC

AS A1 A2 A3 CSIO IACK A12 A13 A14 GND
NC CS0 CS1 NC NC NC NC IACK2 IACK1 VCC

/CS0 = /CSIO * /A14
/CS1 = /CSIO * A14

/IACK1 = /AS * /IACK * /A3 * /A2 * A1
/IACK2 = /AS * /IACK * /A3 * A2 * /A1

DESCRIPTION
DUART board chip-select and IACK decoder

/IACK1 connects to the DUART and responds when IRQ level is 1
/IACK1 connects to the DUART and responds when IRQ level is 2
